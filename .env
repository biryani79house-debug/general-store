# Using the same Railway PostgreSQL database for both development and production
# Remove USE_SQLITE=true to use PostgreSQL instead of SQLite
USE_SQLITE=false
DATABASE_URL=postgresql://postgres:RgsdjQMdqNQdkdsovGfmBOxFgjWXAeNw@hopper.proxy.rlwy.net:17433/railway

# WhatsApp Webhook Configuration
# Configure your webhook URL to receive order notifications
# This can be any service that accepts HTTP POST requests with JSON payload
WHATSAPP_WEBHOOK_URL=http://localhost:8001
WHATSAPP_WEBHOOK_SECRET=my-webhook-secret-123

# Railway Internal Communication
# When deployed on Railway, services communicate via localhost
# WHATSAPP_WEBHOOK_URL=http://localhost:8001

# WhatsApp Configuration
# For security, Twilio credentials are set in Railway environment variables
# NOT in this .env file (which is public on GitHub)
# Set these in Railway dashboard: Variables section
# TWILIO_ACCOUNT_SID=your-twilio-account-sid
# TWILIO_AUTH_TOKEN=your-twilio-auth-token
# TWILIO_WHATSAPP_NUMBER=your-twilio-whatsapp-number

# JWT Secret Key (change this in production)
SECRET_KEY=your-secret-key-change-this-in-production
